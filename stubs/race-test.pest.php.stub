<?php

declare(strict_types=1);

it('finishes concurrent requests without infrastructure or server failures', function (): void {
    $result = race()
        ->participants({{ participants }})
        ->postJson({{ uri }})
        ->startTogether()
        ->run();

    $result
        ->assertAllFinished()
        ->assertNoWorkerFailures()
        ->assertNoTimeouts()
        ->assertNoServerErrors();

    // Add a domain invariant here, such as a final balance, stock, or row count.
});
