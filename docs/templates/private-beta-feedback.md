# RaceProof private-beta feedback

Send this response through the agreed private channel. Replace prompts with
bounded, sanitized values; do not attach raw environment dumps or retained run
directories.

## Setup

- Opaque participant ID:
- RaceProof commit or signed prerelease:
- PHP version:
- Laravel version:
- Database driver and version:
- Platform: Linux / WSL2 / macOS / Windows / other
- Architecture: x86_64 / arm64 / other

## Scenario

- Category: overselling / coupon redemption / wallet debit / quote acceptance /
  unique constraint / lock misuse / deadlock / lock timeout / custom
- Was broken behavior reproduced? yes / no / not attempted
- Was the fix verified? yes / no / not attempted
- Was a regression test added? yes / no
- Iterations completed for each claimed outcome:
- Sanitized expected and observed behavior:

## Product feedback

- Category: API / bug / compatibility / documentation / DX / reporting / other
- Smallest sanitized reproduction:
- What decision or fix would make this actionable?

Do not include names, employer details, email addresses, repository URLs, `.env`
content, connection strings, database names, hostnames, IP addresses, credentials,
headers, session IDs, cookies, tokens, production data, or private vulnerability
details. Use the security channel from `SECURITY.md` for a suspected vulnerability.

Publication is a separate step. Completing this template does not grant consent
to publish any part of it.
